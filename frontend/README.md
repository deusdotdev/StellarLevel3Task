# EQRail frontend

Vite + React UI for the Testnet desk. See the [root README](../README.md) for architecture and deploy.

```bash
npm install
npm test
npm run dev
```

Copy `../deployments/testnet.env` into `.env.local` (the deploy script does this for you).
